how_many_years = float(input("Years"))

year_seconds = 31536000 #(60*60*24*365)
birth_rate = year_seconds / 7 #(seconds)
death_rate = year_seconds / 13 #(seconds)
immigrant_rate = year_seconds / 35 #(seconds)

if
    population_change = (birth_rate * how_many_years) - (death_rate * how_many_years) + (immigrant_rate * how_many_years)
    #This is the formula for the population change

else:
    (how_many_years <0):
    print ("Invalid input!")
    
# population_america = 307357870



# population_change = (birth_rate + immigrant_rate - death_rate)

print (population_change)
